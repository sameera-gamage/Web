{
  "name": "rapidstudio-php-assets",
  "private": true,
  "type": "module",
  "description": "Build step for the PHP site's CSS and JS. Only needed when the design changes — the site itself runs with no Node.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "lenis": "^1.1.18"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "tailwindcss": "^3.4.17"
  }
}
